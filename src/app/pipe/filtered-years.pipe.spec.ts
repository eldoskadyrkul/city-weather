import { FilteredYearsPipe } from './filtered-years.pipe';

describe('FilteredYearsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredYearsPipe();
    expect(pipe).toBeTruthy();
  });
});
